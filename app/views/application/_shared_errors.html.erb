<% if obj.errors.any? %>
  <ul class="list-group">
    <% obj.errors.each do |attribute, message| %>
      <small class="form-text text-danger"><%= "#{attribute unless attribute == :user_id} #{message}" %></small>
      
    <% end %>
  </ul>
<% end %>
